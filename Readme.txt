Simple HTTP proxy that caches web objects.

1)Proxy to accept incoming connections, read and parse requests, forward requests to web servers, read the servers’ responses, and forward those responses to the corresponding clients

2)proxy to deal with multiple concurrent connections.

3)added caching to proxy using a simple main memory cache of recently accessed web content